# =====================================================================
# 固定能量含反应坐标 —— 规范整理输入
# 源输入: regression/cases/p26_fixed_energy_rc/input_qct.txt
# 整理说明: 按 &SYSTEM / &DYNAMICS / &OUTPUT 三块重排，逐关键字中文注释；
#           关键字值与源输入一致。TITLE1 已语义化（源输入标题含工程编号 P2.6）。
# =====================================================================

# ===== &SYSTEM =====   系统定义 + 电子结构
# 模型预设：气相碰撞（无表面）
MODEL = GAS-PHASE
# 标题行（语义化）
TITLE1 = Fixed-energy incl. reaction coordinate - LEPS equilateral H3, ENMT=20, NT=1000
# 任务类型：势垒起始（NBAR=3 自动触发，见说明文档）
TASK = BARRIER
# 体系无关测试势：London-Eyring-Polyanyi-Sato 三原子势
TEST_PES = LEPS
LEPS_DE = 4.746             # LEPS 离解能 De [eV]
LEPS_RE = 1.401             # LEPS 平衡键长 re [Å]
LEPS_A = 1.028              # LEPS Morse 指数 a [Å⁻¹]
LEPS_DELTA = 0.164          # LEPS Sato 参数 Δ
NATOMS = 3                  # 原子总数（三原子）
ATOM_MASSES = 1.008, 1.008, 1.008   # 原子质量 [u]（H₃ 类等边体系）
NATOMA = 3                  # 片段 A 原子数（全部原子）
NLINA = 0                   # 片段 A 线型标志：0 = 非线性（等边 H₃，无弯曲伪模）
NATOMB = 0                  # 片段 B 原子数（无）
# 电子方法：绝热经典
ELEC_METHOD = ADIABATIC

# ===== &DYNAMICS =====   动力学方法 + 初始条件
# 积分器：Verlet
INTEGRATOR = VERLET
ISEED = 20260829            # 随机种子（固定 → 双跑逐位一致）
NT = 1000                   # 轨迹数
NS = 5                      # 每轨迹 MD 步数
DT = 0.01                   # 时间步 [10 fs]
# 片段 A 初始采样：固定能量含反应坐标（NACTA=6）
INIT_SAMPLING_A = FIXED-ENERGY
ENMT_A = 20.0               # 固定总内能 ENMT [kcal/mol]（高于阱底，含 ZPE）
# NBAR=3: 微正则反应坐标能量采样（手册 V.14 635-640 行）；SELECT.f90 将 NACTA
# 强制为 6。等边 H₃（NLINA=0）无弯曲伪模，Beyer-Swinehardt 网格 NSTEP 守卫可通过
# （共线构型有 0.5134 cm⁻¹ 弯曲伪模 → STEP=0.05134 → NSTEP 溢出停机，登记 F22）。
NBAR = 3                    # 反应坐标能量采样方式：3 = 微正则（固定总能量）
IJDIR = 0                   # 反应坐标初速度方向标志：0 = 关闭（IJDIR 分支探针见说明）
IDIR = 0                    # 方向标志（与 IJDIR 配合，本 case 未启用）
JDIR = 0                    # 方向标志（与 IJDIR 配合，本 case 未启用）
QZA_EQ = 0.0, 1.016102, 0.0, -0.879955, -0.508051, 0.0, 0.879955, -0.508051, 0.0   # 等边三角形平衡构型（LEPS 全局极小，边长 1.759910 Å）

# ===== &OUTPUT =====     输出控制
NCOOR = 1                   # 坐标输出参数
