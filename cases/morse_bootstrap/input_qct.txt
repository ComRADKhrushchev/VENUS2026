# =====================================================================
# TEST 势 Morse 自举 —— 规范整理输入
# 源输入: regression/cases/test_morse_1traj/input_qct.txt
# 整理说明: 按 &SYSTEM / &DYNAMICS / &OUTPUT 三块重排，逐关键字中文注释；
#           关键字值与源输入一致。TITLE1 已语义化（源输入标题含工程编号 P1.2，
#           此处去掉；TITLE1 仅打印用，不影响计算）。
# =====================================================================

# ===== &SYSTEM =====   系统定义 + 电子结构
# 模型预设：气相碰撞（无表面）
MODEL = GAS-PHASE
# 标题行（语义化；仅打印用）
TITLE1 = TEST PES: Morse H2 diatomic, single trajectory
# 体系无关测试势：Morse 双原子
TEST_PES = MORSE
# MORSE 参数未显式指定——程序使用内置默认（H₂：De=4.746 eV、re=1.401 Å、
# a=1.028 Å⁻¹），与源输入一致（源输入也未显式指定）。
NATOMS = 2                  # 原子总数（H₂）
ATOM_MASSES = 1.008, 1.008  # 原子质量 [u]（氢）
NATOMA = 1                  # 片段 A 原子数（H）
NATOMB = 1                  # 片段 B 原子数（H）
# 电子方法：绝热经典
ELEC_METHOD = ADIABATIC

# ===== &DYNAMICS =====   动力学方法 + 初始条件
# 任务类型：经典轨迹
TASK = TRAJECTORY
# 积分器：Verlet
INTEGRATOR = VERLET
ISEED = 12345               # 随机种子
NT = 1                      # 轨迹数
NS = 5000                   # 每轨迹 MD 步数（总时长 5 ps）
DT = 0.01                   # 时间步 [10 fs]

# ===== &OUTPUT =====     输出控制
NCOOR = 1                   # 坐标输出参数

# ===== 源输入注释（保留参考）=====
# 原输入含以下说明（见源文件 input_qct.txt）：
#   - 体系无关势（无 Analytic_Potential.txt、无 nn_weights）：
#     TEST_PES=MORSE 完全由运行时关键字指定。
#   - 解析参照：E(n=0) 距阱底 = we/2 − wexe/8，
#     we = a·sqrt(2·De/μ)（代码单位制）。
