# fixed_normalmode_qnums — 固定简正模量子数采样

> 运行：`cd cases/fixed_normalmode_qnums && ../../venus_test.e`（主输入文件 `input_qct.txt`）

## 1. 目的

验证固定简正模量子数采样（`INIT_SAMPLING_A=NORMAL-MODE`）的正确性：
量子数打印与输入一致、谐波能级能量解析闭合、能量固定而相位随机
（能量高度集中、逐轨迹展宽极小）。

## 2. 理论与体系

体系与谱：
- 谱为 E′ 二重简并对 1013.5 cm⁻¹ 与 A₁′ 呼吸模 1803.8 cm⁻¹（stdout 频谱表）。

采样流程（与微正则简正模采样相同的 `NMODE` → `INITQP` 流程，仅能量分配不同）：
- 每模取谐波能级 (n_i+½)ℏω_i，谐波能级和
  E = [1.5×1013.47 + 2.5×1013.51 + 3.5×1803.83] / 349.755 = 29.64 kcal/mol
  （349.755 为 cm⁻¹→kcal/mol 换算因子）。
- 能量闭合缩放至完整非谐 LEPS 势。
- 随机相位由 `INITQP` 逐轨迹重抽——能量固定、相位随机，是固定量子数语义的关键。

## 3. 方法与流程

1. 读取输入并打印 NORMAL MODE SAMPLING 与量子数表（stdout，results.txt）。
2. 每轨迹传播 NS=10 步并打印相空间（fort.1001 起每文件一条轨迹）。

## 4. 核心验证

见 `fig_sampling_stats.png`。

> 图注：左面板为 1000 条轨迹 EVIBA 的经验累积分布（ECDF），右面板为其分布
> 集中度（固定量子数 ANQ_A=(1,2,3) 下能量唯一、相位是唯一自由度）。
> 判定依据：ANQ 行每轨迹 1.00 2.00 3.00 与输入一致；EVIBA 均值 29.636 vs
> 理论对照值 29.642 kcal/mol（相对偏差 0.0010，门限 0.5%）；EVIBA 展宽
> 0.059 kcal/mol（< 0.1）。
