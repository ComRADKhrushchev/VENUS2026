# =====================================================================
# 积分器矩阵 —— 规范整理输入（VERLET 10 ps 长时间基线）
# 源输入: regression/cases/p42_integrator_matrix/input_qct.txt
# 整理说明: 按 &SYSTEM / &DYNAMICS / &OUTPUT 三块重排，逐关键字中文注释；
#           关键字值与源输入一致。TITLE1 已语义化（源输入标题含工程编号 P4.2）。
#           本输入为 8 入口扫描的 Tier B 双跑门禁基线；完整扫描由 sweep.py
#           驱动（见说明文档 §4）。
# =====================================================================

# ===== &SYSTEM =====   系统定义 + 电子结构
# 模型预设：气相碰撞（无表面）
MODEL = GAS-PHASE
# 标题行（语义化）
TITLE1 = Integrator matrix — VERLET long-run baseline (10 ps)
# 任务类型：经典轨迹
TASK = TRAJECTORY
# 体系无关测试势：简谐势
TEST_PES = HARMONIC
HARM_K = 1.0, 4.0, 9.0      # 三简正方向力常数 k [eV/Å²]（非简并三频阱）
HARM_X0 = 0.0, 0.0, 0.0     # 简谐势平衡位置 [Å]
NATOMS = 2                  # 原子总数（双原子）
ATOM_MASSES = 1.008, 1.008  # 原子质量 [u]
NATOMA = 1                  # 片段 A 原子数
NATOMB = 1                  # 片段 B 原子数
# 电子方法：绝热经典
ELEC_METHOD = ADIABATIC

# ===== &DYNAMICS =====   动力学方法 + 初始条件
# 积分器：Verlet（10 ps 长时间基线）
INTEGRATOR = VERLET
ISEED = 20260820            # 随机种子
NT = 1                      # 轨迹数
NS = 100000                 # 每轨迹 MD 步数（10 ps 长跑）
DT = 0.01                   # 时间步 [10 fs]
RMAX = 20.0                 # 最大碰撞参数 [bohr]
RBAR = 15.0                 # 非反应截断势垒半径 [bohr]

# ===== &OUTPUT =====     输出控制
NIP = 1000                  # 输出间隔步数（须为 7 的倍数——ADAMS 宏步门限）
NCOOR = 1                   # 坐标输出参数

# ===== 扫描说明（详见说明文档 §4）=====
# sweep.py（非单输入协议）生成 8 积分器入口 × DT 扫描 × 长跑矩阵；
# 本 input.txt 为 VERLET 10 ps 基线（Tier B 双跑门禁）。
